# =============================================================================
# Fake Driver Simulator Configuration
# =============================================================================

# SvelteKit API URL (used by multi-driver mode to fetch routes)
API_URL=http://localhost:5173

# WebSocket server URL (without path)
WS_URL=ws://localhost:3001

# =============================================================================
# Single Driver Mode (npm run dev)
# =============================================================================

# Driver to simulate (from your seeded data)
# Available drivers after seeding:
#   - drv_sample_001 (Mike Johnson - Van)
#   - drv_sample_002 (Sarah Williams - Sprinter)
#   - drv_test_001 (Test Driver One - Box Truck)
#   - drv_test_002 (Test Driver Two - Cargo Bike)
DRIVER_ID=drv_sample_001

# Tenant ID for the driver
TENANT_ID=tenant_test_001

# =============================================================================
# Multi-Driver Mode (npm run dev:multi)
# =============================================================================

# Multi-driver mode fetches active routes from the API.
# Drivers are discovered automatically - no need to configure manually.
# The simulator will find all routes with status "in_progress" for the
# specified tenant and create a simulator for each assigned driver.

# =============================================================================
# Simulation Settings
# =============================================================================

# How often to send location pings (milliseconds)
PING_INTERVAL_MS=3000

# Simulation mode: "stationary" | "linear" | "delivery_route"
# - stationary: Stay in one place (good for basic testing)
# - linear: Move in a straight line
# - delivery_route: Follow a realistic delivery route pattern
SIM_MODE=delivery_route

# Starting location (lat,lng) - only used for single-driver mode
# Default: Manhattan area (matches seed data)
START_LAT=40.7484
START_LNG=-73.9857

# Speed in meters per second (for moving modes)
# 8 m/s ≈ 18 mph (city driving with stops)
# 10 m/s ≈ 22 mph (faster suburban driving)
SPEED_MPS=8
